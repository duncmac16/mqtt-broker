{
  "name": "broker",
  "version": "1.0.0",
  "description": "MQTT Broker for heroku",
  "main": "broker.js",
  "scripts": {
    "start": "node broker.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aedes": "^0.43.0"
  }
}
